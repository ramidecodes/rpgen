Add a new tool for the GMA to generate suggested actions for the user.
The actions should be effimeral and only short lived. We should display the suggested actions as clickable buttons in the text area. WEhen user clicks on one of them they get added as text in the user text message area and immediately send as response from the user.
